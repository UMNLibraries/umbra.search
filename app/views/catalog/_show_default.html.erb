<%# default partial to display solr document fields in catalog show view -%>

<%
 img = document.fetch('object_s', false).to_s
 thumb_url = document.fetch('isShownAt_s', false)
 description = document.fetch('sourceResource_collection_description_txt', false)
%>

<%- if img %>
<div class="document-thumbnail search-thumbnail">
  <img src="<%= "#{img}" %>">
</div>
<%- end %>

<dl class="dl-horizontal  dl-invert">
  <% if thumb_url %>
    <dt class="blacklight-is_shown_at">View At:&nbsp;</dt>
    <dd class="blacklight-is_shown_at"><a href="<%= document['isShownAt_s'] %>"><%= thumb_url %></a></dd>
  <% end %>

  <% document_show_fields(document).each do |solr_fname, field| -%>
    <% if should_render_show_field? document, field %>
      <dt class="blacklight-<%= solr_fname.parameterize %>"><%= render_document_show_field_label document, :field => solr_fname %>&nbsp; </dt>
      <dd class="blacklight-<%= solr_fname.parameterize %>"><%= render_document_show_field_value document, :field => solr_fname %></dd>
    <% end -%>
  <% end -%>


  <% if description %>
    <span class="section-label">Description</span>
    <div class="well">
       <%= description %>
    </div>
  <% end %>
</dl>
