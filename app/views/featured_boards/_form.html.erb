<%= form_for(@featured_board) do |f| %>
  <% if @featured_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@featured_board.errors.count, "error") %> prohibited this Featured Image from being saved:</h2>

      <ul>
      <% @featured_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">

      <div class="field">
        <%= f.label :title %><br />
        <%= f.text_field :title %>
      </div>

      <div class="field">
        <%= f.label :url, "Board URL" %><br />
        <%= f.text_field :url %>
      </div><br />


      <div class="field">
        <%= f.label :published %><br />
        <%= f.check_box :published %>
      </div>

      <p><%= f.submit ( f.object.new_record? ? "Create Featured Board" : "Save Changes"), class: "btn btn-primary"  %></p>
      <% unless @featured_board.new_record? %>
        <%#= link_to 'Delete this Image', @featured_board, method: :delete, data: { confirm: 'Are you sure you want to delete this featured image? You cannot undo this action.' }, class:"btn btn-danger" %>
      <% end %>
    </div>
<% end %>
